import React from "react";

const Body = () => {
  const sectionStyle = {
    height: "100vh",
    width: "100vh",
    backgroundColor: "red",
  };

  return (
    <div id="body" style={sectionStyle}>
      Body Section
    </div>
  );
};

export default Body;
