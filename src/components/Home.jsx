import React from "react";

const Home = () => {
  const sectionStyle = {
    height: "100vh",
    width: "100vh",
    backgroundColor: "blue",
  };

  return (
    <div id="home" style={sectionStyle}>
      Home Section
    </div>
  );
};

export default Home;
